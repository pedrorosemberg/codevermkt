# Ativa o módulo de reescrita
RewriteEngine On

# Remove a extensão .html das URLs
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*[^/])$ /$1.html [L]

# Redireciona URLs com .html para URLs sem a extensão
RewriteCond %{THE_REQUEST} \s/+([^\s]+)\.html [NC]
RewriteRule ^ /%1 [R=301,L]
